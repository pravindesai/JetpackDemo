<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jetpackdemo">

    <application
        android:name=".BaseClasses.JetpackApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetpackDemo">
        <activity android:name=".AndroidSqliteDb.SqliteActivity"></activity>
        <activity android:name=".workManager.workmanagerActivity" />
        <activity android:name=".paging.Paging_Activity" />
        <activity android:name=".retorfit_demo.Retrofit_Activity" />
        <activity android:name=".mvvm_api.Mvvm_api" />
        <activity android:name=".vM_and_Room.VM_And_Room" />
        <activity android:name=".ViewModel.VMActivity" />
        <activity android:name=".databinding_Nav.DBin_Nav" />
        <activity android:name=".listDataBinding.ListViewDataBinding" />
        <activity android:name=".dataBindingDemo.DadaBindingAct" />
        <activity android:name=".NavigationComponentDemo.NavigationDemo" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>